{
  "name": "ai-detective-k-blockchain",
  "version": "1.0.0",
  "description": "Hardhat project for AI-Detective-K forensic chain-of-custody smart contract",
  "scripts": {
    "compile": "hardhat compile",
    "deploy:coston2": "hardhat run scripts/deploy.js --network coston2",
    "test:coston2": "hardhat run scripts/test-interaction.js --network coston2",
    "clean": "hardhat clean"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.0",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.0"
  }
}
